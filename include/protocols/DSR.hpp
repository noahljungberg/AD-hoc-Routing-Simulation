/*
The source node determines the entire route to the destination. By sending a Route Request (RREQ) and waiting for the Route Reply (RREP). Then it creates a vector with the nodes and at each node, they check their index and what the next node is.
*/